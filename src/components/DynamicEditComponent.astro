---
import type { Comment } from "../types/comments-types";
import EditSingleComment from "./EditSingleComment";

export const prerender = false;

const { id } = Astro.props

---

<section>
    <EditSingleComment paramId={id} client:idle  />
</section>